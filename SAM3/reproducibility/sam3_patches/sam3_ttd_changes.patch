diff --git a/sam3/eval/coco_eval_offline.py b/sam3/eval/coco_eval_offline.py
index ec8a0c0..5f93366 100644
--- a/sam3/eval/coco_eval_offline.py
+++ b/sam3/eval/coco_eval_offline.py
@@ -12,6 +12,7 @@ we may need to split the inference process for a given image in several chunks.
 
 import logging
 from collections import defaultdict
+import json
 
 import torch
 from pycocotools.coco import COCO
@@ -146,6 +147,17 @@ class CocoEvaluatorOfflineWithPredFileEvaluators:
         logging.info("OfflineCoco evaluator: Loading groundtruth")
         self.gt = COCO(self.gt_path)
 
+        with open(dumped_file, "r") as f:
+            predictions = json.load(f)
+        if len(predictions) == 0:
+            logging.info(
+                "Coco evaluator: prediction file is empty; returning zero-valued COCO metrics"
+            )
+            return {
+                f"coco_eval_{self.iou_type}_{metric_name}": 0.0
+                for metric_name in COCO_METRICS
+            }
+
         # Creating the result file
         logging.info("Coco evaluator: Creating the result file")
         cocoDt = self.gt.loadRes(str(dumped_file))
diff --git a/sam3/eval/coco_writer.py b/sam3/eval/coco_writer.py
index f49fcf2..73561a8 100644
--- a/sam3/eval/coco_writer.py
+++ b/sam3/eval/coco_writer.py
@@ -306,6 +306,12 @@ class PredictionDumper:
             if len(prediction) == 0:
                 continue
 
+            # Some validation predictions can legitimately contain only scores /
+            # labels (for example negative images filtered by presence) and no
+            # actual segmentation payload. Treat those as empty predictions.
+            if "masks" not in prediction and "masks_rle" not in prediction:
+                continue
+
             scores = prediction["scores"].tolist()
             labels = prediction["labels"].tolist()
 
diff --git a/sam3/model/vitdet.py b/sam3/model/vitdet.py
index f6771a1..c40810d 100644
--- a/sam3/model/vitdet.py
+++ b/sam3/model/vitdet.py
@@ -68,7 +68,13 @@ class Mlp(nn.Module):
         self.drop2 = nn.Dropout(drop_probs[1])
 
     def forward(self, x):
-        x = addmm_act(type(self.act), self.fc1, x)
+        # The fused addmm+activation kernel is inference-only; training needs
+        # the regular autograd-tracked path.
+        if torch.is_grad_enabled():
+            x = self.fc1(x)
+            x = self.act(x)
+        else:
+            x = addmm_act(type(self.act), self.fc1, x)
         x = self.drop1(x)
         x = self.norm(x)
         x = self.fc2(x)
diff --git a/sam3/perflib/fused.py b/sam3/perflib/fused.py
index 6800cca..682d1e9 100644
--- a/sam3/perflib/fused.py
+++ b/sam3/perflib/fused.py
@@ -10,6 +10,7 @@ addmm_act_op = torch.ops.aten._addmm_activation
 def addmm_act(activation, linear, mat1):
     if torch.is_grad_enabled():
         raise ValueError("Expected grad to be disabled.")
+    output_dtype = linear.weight.dtype
     self = linear.bias.detach()
     mat2 = linear.weight.detach()
     self = self.to(torch.bfloat16)
@@ -18,8 +19,8 @@ def addmm_act(activation, linear, mat1):
     mat1_flat = mat1.view(-1, mat1.shape[-1])
     if activation in [torch.nn.functional.relu, torch.nn.ReLU]:
         y = addmm_act_op(self, mat1_flat, mat2.t(), beta=1, alpha=1, use_gelu=False)
-        return y.view(mat1.shape[:-1] + (y.shape[-1],))
+        return y.view(mat1.shape[:-1] + (y.shape[-1],)).to(output_dtype)
     if activation in [torch.nn.functional.gelu, torch.nn.GELU]:
         y = addmm_act_op(self, mat1_flat, mat2.t(), beta=1, alpha=1, use_gelu=True)
-        return y.view(mat1.shape[:-1] + (y.shape[-1],))
+        return y.view(mat1.shape[:-1] + (y.shape[-1],)).to(output_dtype)
     raise ValueError(f"Unexpected activation {activation}")
diff --git a/sam3/train/matcher.py b/sam3/train/matcher.py
index 5adc6a4..0939268 100644
--- a/sam3/train/matcher.py
+++ b/sam3/train/matcher.py
@@ -14,6 +14,10 @@ from torch import nn
 
 
 def _do_matching(cost, repeats=1, return_tgt_indices=False, do_filtering=False):
+    # Hungarian matching cannot handle NaN/inf entries. Treat them as invalid
+    # assignments with a very large cost so training can continue.
+    if not np.isfinite(cost).all():
+        cost = np.nan_to_num(cost, nan=1e9, posinf=1e9, neginf=1e9)
     if repeats > 1:
         cost = np.tile(cost, (1, repeats))
 
diff --git a/sam3/train/trainer.py b/sam3/train/trainer.py
index cc6c37e..ea62868 100644
--- a/sam3/train/trainer.py
+++ b/sam3/train/trainer.py
@@ -436,14 +436,29 @@ class Trainer:
     def _load_resuming_checkpoint(self, ckpt_path: str):
         logging.info(f"Resuming training from {ckpt_path}")
 
-        with g_pathmgr.open(ckpt_path, "rb") as f:
-            checkpoint = torch.load(f, map_location="cpu")
+        if self.mode == "val" and os.path.isfile(ckpt_path):
+            # Eval only needs model weights. The training checkpoint also contains
+            # optimizer state and can be very large, so mmap keeps eval from
+            # eagerly loading tensors it will never use.
+            checkpoint = torch.load(
+                ckpt_path, map_location="cpu", mmap=True, weights_only=False
+            )
+        else:
+            with g_pathmgr.open(ckpt_path, "rb") as f:
+                checkpoint = torch.load(f, map_location="cpu")
         load_state_dict_into_model(
             model=self.model,
             state_dict=checkpoint["model"],
             ignore_missing_keys=self.checkpoint_conf.skip_saving_parameters,
         )
 
+        if self.mode == "val":
+            self.epoch = checkpoint.get("epoch", self.epoch)
+            self.steps = checkpoint.get("steps", self.steps)
+            self.ckpt_time_elapsed = checkpoint.get("time_elapsed", 0) or 0
+            self.best_meter_values = checkpoint.get("best_meter_values", {})
+            return
+
         self.optim.optimizer.load_state_dict(checkpoint["optimizer"])
         self.loss.load_state_dict(checkpoint["loss"], strict=True)
         self.epoch = checkpoint["epoch"]
